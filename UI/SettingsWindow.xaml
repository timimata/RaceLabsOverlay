<Window x:Class="RaceLabsOverlay.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="500" Width="600"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Background="#1A1A1A">

    <Window.Resources>
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" Background="#222222" BorderBrush="#333333" BorderThickness="0,0,0,2" Padding="{TemplateBinding Padding}">
                            <ContentPresenter Content="{TemplateBinding Header}" TextBlock.Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="LimeGreen"/>
                                <Setter TargetName="Border" Property="Background" Value="#2A2A2A"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#333333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0" Background="Transparent" BorderThickness="0">
            <TabItem Header="General">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="20">
                    <StackPanel>
                        <TextBlock Text="Startup" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                        <CheckBox x:Name="StartWithWindowsCheck" Content="Start with Windows" Foreground="#CCCCCC" Margin="0,5"/>
                        <CheckBox x:Name="CheckUpdatesCheck" Content="Check for updates on startup" Foreground="#CCCCCC" Margin="0,5"/>
                        <CheckBox x:Name="MinimizeToTrayCheck" Content="Minimize to system tray" Foreground="#CCCCCC" Margin="0,5"/>
                        <TextBlock Text="Updates" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,20,0,10"/>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="Update Channel:" Foreground="#CCCCCC" Width="120" VerticalAlignment="Center"/>
                            <ComboBox x:Name="UpdateChannelCombo" Width="150" Background="#333333" Foreground="White">
                                <ComboBoxItem Content="Stable"/>
                                <ComboBoxItem Content="Beta"/>
                                <ComboBoxItem Content="Development"/>
                            </ComboBox>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Overlay">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="20">
                    <StackPanel>
                        <TextBlock Text="Appearance" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="Opacity:" Foreground="#CCCCCC" Width="120" VerticalAlignment="Center"/>
                            <Slider x:Name="OpacitySlider" Width="200" Minimum="0.3" Maximum="1.0" Value="0.95" TickFrequency="0.05"/>
                            <TextBlock x:Name="OpacityValue" Text="95%" Foreground="White" Margin="10,0,0,0"/>
                        </StackPanel>
                        <CheckBox x:Name="ClickThroughCheck" Content="Click-through by default" Foreground="#CCCCCC" Margin="0,10"/>
                        <TextBlock Text="Ghost Racing" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,20,0,10"/>
                        <CheckBox x:Name="EnableGhostCheck" Content="Enable ghost recording" Foreground="#CCCCCC" Margin="0,5"/>
                        <CheckBox x:Name="AutoSaveGhostCheck" Content="Auto-save ghost on best lap" Foreground="#CCCCCC" Margin="0,5"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Hotkeys">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="20">
                    <StackPanel>
                        <TextBlock Text="Keyboard Shortcuts" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Toggle Edit Mode:" Foreground="#CCCCCC"/>
                            <TextBox Grid.Column="1" x:Name="EditModeHotkey" Text="Ctrl+Shift+O" Background="#333333" Foreground="White"/>
                        </Grid>
                        <Grid Margin="0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Toggle Click-Through:" Foreground="#CCCCCC"/>
                            <TextBox Grid.Column="1" x:Name="ClickThroughHotkey" Text="Ctrl+Shift+H" Background="#333333" Foreground="White"/>
                        </Grid>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Record Ghost:" Foreground="#CCCCCC"/>
                            <TextBox Grid.Column="1" x:Name="RecordGhostHotkey" Text="Ctrl+Shift+R" Background="#333333" Foreground="White"/>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="About">
                <StackPanel Margin="20" HorizontalAlignment="Center">
                    <TextBlock Text="🏎️" FontSize="48" HorizontalAlignment="Center"/>
                    <TextBlock Text="RaceLabs Overlay" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,20,0,5"/>
                    <TextBlock x:Name="VersionText" Text="Version 0.1.0" FontSize="14" Foreground="#888888" HorizontalAlignment="Center"/>
                    <TextBlock Text="Professional iRacing Telemetry Overlay" FontSize="12" Foreground="#666666" HorizontalAlignment="Center" Margin="0,20,0,0"/>
                    <TextBlock Text="© 2026 Tiago" FontSize="11" Foreground="#555555" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                    <Button x:Name="CheckUpdateButton" Content="Check for Updates" Width="150" Margin="0,30,0,0" Background="#333333" Foreground="White" Click="CheckUpdateButton_Click"/>
                </StackPanel>
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20">
            <Button Content="Save" Width="80" Height="30" Margin="0,0,10,0" Background="#228822" Foreground="White" Click="SaveButton_Click"/>
            <Button Content="Cancel" Width="80" Height="30" Background="#333333" Foreground="White" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
